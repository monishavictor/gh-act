# .github/actions/common-action-1.yml
name: "common1"
description: "common method 1"
inputs:
  name:
    required: true
    description: "Hello action"
runs:
  using: "composite"
  steps:
    - run: echo "Hello, ${{ inputs.name }}!"
